sudo: required

language: c++

services:
        - docker

before_install:
        - docker pull valearna/ubuntu-tpc-base
        - docker run -d -p 127.0.0.1:80:4567 valearna/ubuntu-tpc-base /bin/sh -c "ls -l"
